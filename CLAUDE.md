# Capybara Onsen — 작업 원칙

## 에셋 원본 보호
- `public/assets/`의 PNG 등 에셋 원본 파일은 **절대 직접 수정하지 않는다**.
- 가공이 필요하면 사본을 만들어 작업한다 (예: `capybara.png` → `capybara-back.png`).
- 원본 복구가 필요한 경우 `~/Downloads/` 등 외부 소스에서 다시 추출한다.

## 비주얼 자가검증
- 모든 비주얼 수정 후에는 **브라우저 스크린샷으로 자가검증** 필수.
- "완료" 보고 전에 반드시 실제 화면에서 확인한다.
- 스크린샷은 관련 기능이 보이는 각도/상태에서 찍는다.

## 수정사항 반영 원칙
- 요청된 수정사항은 **빠짐없이 전부 반영**한다.
- 반영하지 못한 항목이 있으면 **명시적으로 보고**한다.
- 추측으로 고치지 말고 원인을 확인한 뒤 수정한다.

## 기술 스택
- Three.js (r185) + Vite
- 비주얼 스타일: 무광 툰 셰이딩 (MeshToonMaterial), 카피바라 스프라이트 기준
- 팔레트: 저채도 포레스트 (P 객체), 네온/발광 금지
- 포스트프로세싱: RenderPixelatedPass (PIXEL_SIZE=4)

## 디버그 유틸
- `window.__toggleRays(bool)` — 빛줄기 토글
- `window.__teleport(x, z)` — 카피바라 텔레포트
